services:
  postgres:
    image: postgres:18
    container_name: postgres
    env_file:
      - ./.env
    ports:
      - '5492:5432'
    volumes:
      - postgresDb:/var/lib/postgresql
volumes:
  postgresDb: